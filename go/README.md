# 🐹 Go examples

Part A of the book (chapters 2–9). One Go module; each example is its own `package main`.

```bash
go run ./<chapter-folder>/<example-folder>
```

Third-party dependencies (declared in `go.mod`): `fatih/color`, `gorilla/mux`, `mattn/go-sqlite3`.
