# Chapter 19 — Building Http Services In Rust

Examples in this crate:

- `--example routes_and_handlers` — 19.2.1 Routes and Handlers
- `--example a_minimal_url_shortener_in_rust` — Exercise: A Minimal Url Shortener In Rust

Run one with `cargo run -p ch19-building-http-services-in-rust --example <name>` from the `rust/` directory.
