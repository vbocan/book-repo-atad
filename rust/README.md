# 🦀 Rust examples

Part B of the book (chapters 10–19), as a Cargo workspace with one crate per chapter. Each chapter's programs are Cargo *examples*.

```bash
cargo run -p <chapter-crate> --example <name>
```
