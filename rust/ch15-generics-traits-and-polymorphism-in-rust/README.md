# Chapter 15 — Generics Traits And Polymorphism In Rust

Examples in this crate:

- `--example a_pluggable_logging_system` — Exercise: A Pluggable Logging System

Run one with `cargo run -p ch15-generics-traits-and-polymorphism-in-rust --example <name>` from the `rust/` directory.
