# Chapter 14 — Enums Pattern Matching And Error Handling In Rust

Examples in this crate:

- `--example configuration_file_parser` — Exercise: Configuration File Parser

Run one with `cargo run -p ch14-enums-pattern-matching-and-error-handling-in-rust --example <name>` from the `rust/` directory.
